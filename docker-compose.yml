services:
  app:
    build: .
    container_name: app-sveltekit
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
